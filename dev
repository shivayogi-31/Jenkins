pipeline {
    agent any
      stages {
          stage ('Build Stage') {
              steps {
                    echo 'build stage running'
                    echo 'deployed to dev environment'
              }
          }
      }
}
